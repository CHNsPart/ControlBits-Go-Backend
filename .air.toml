root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/api ./cmd/api"
  bin = "./tmp/api"
  delay = 1000
  include_ext = ["go"]
  exclude_dir = ["tmp", "migrations", "vendor"]

[log]
  time = true

[misc]
  clean_on_exit = true
